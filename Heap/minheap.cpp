#include <iostream>
#include <queue>
using namespace std;



int main() {

    // priority_queue<int, vector<int>, greater<int> > pq;
    // pq.push(100);
    // pq.push(90);
    // pq.push(80);

    // cout << "top element of the heap is: " << pq.top() << endl;
    // pq.pop();
    // cout << "Top element of the heap is: " << pq.top() << endl;
    // cout << "size: " << pq.size() << endl;

    return 0;
}